# Leave unset for local dev -- vite.config.ts proxies API calls to
# http://127.0.0.1:8000 (run `uvicorn slate.api:app --reload` for that).
#
# Set this to the deployed backend when building for production, e.g.:
# VITE_API_BASE=https://slate-theta-two.vercel.app
VITE_API_BASE=
