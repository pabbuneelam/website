rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ponytail: one doc per user, owner-only. Google sign-in later keeps the same uid via linkWithPopup.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
